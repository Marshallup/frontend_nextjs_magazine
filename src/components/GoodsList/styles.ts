import { FormControl } from "@mui/material";
import styled from "styled-components";

export const ControlForm = styled(FormControl)`
    min-width: 220px;
`;
export const GoodsListWrapper = styled.div`
    
`;
export const GoodsListHeaderWrapper = styled.div`
    padding-bottom: 20px;
`;